# App Store Connect metadata (Bookmarked +)

Fastlane-style English (US) listing copy for ASC app ID `6791755747`.

Uploaded to App Store Connect for version **1.0** (`PREPARE_FOR_SUBMISSION`).

## Notes

- `release_notes.txt` (What's New) is versioned here for the first release, but ASC rejects editing `whatsNew` while the first version is still preparing for submission. Paste manually after the first update if needed.
- Screenshots live under `../screenshots/` (`iphone-67/`, `iphone-61/`, `iphone-61-asc/`). Uploaded to ASC as **APP_IPHONE_67** (1320×2868) and **APP_IPHONE_61** (1179×2556), 5 each.
- Age rating questionnaire, App Privacy nutrition labels, categories, build selection, copyright, and content rights were set via ASC iris session (not these text files).
