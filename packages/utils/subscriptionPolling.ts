/** Poll interval while waiting for webhook to activate Premium after checkout. */
export const SUBSCRIPTION_ACTIVATION_POLL_INTERVAL_MS = 1500;

/** Stop showing the activating banner after this duration; polling continues in background. */
export const SUBSCRIPTION_ACTIVATION_POLL_TIMEOUT_MS = 45_000;
