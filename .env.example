# Local-only secrets (copy to `.env` — gitignored). Never commit real values.

# Optional: Supabase CLI personal access token (sbp_...) for non-interactive shells.
# Prefer `supabase login` in Terminal (stores in macOS Keychain) when possible.
# SUPABASE_ACCESS_TOKEN=
